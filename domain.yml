version: "3.1"

intents:
  - greet
  - out_of_context
  - question

actions:
  - action_greet
  - action_bot_response
  - action_ask_question

session_config:
  session_expiration_time: 0
  carry_over_slots_to_new_session: true
